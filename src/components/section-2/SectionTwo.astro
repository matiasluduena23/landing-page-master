---
import { Image } from 'astro:assets';
import imgPeople from '../../assets/illustration-stay-productive.png';
import arrow from '../../assets/icon-arrow.svg';
import Testimonial from './Testimonial.astro';
---

<section>
    <div class="container">
        <div class="main-content">
            <Image
                class="main-content-img"
                src={imgPeople}
                alt="three persons working "
            />
            <div class="main-content-text">
                <h2>Stay productive, wherever you are</h2>
                <p>
                    Never let location be an issue when accessing your files.
                    Fylo has you covered for all of your file storage needs.
                </p>
                <p>
                    Securely share files and folders with friends, family and
                    colleagues for live collaboration. No email attachments
                    required.
                </p>
                <a href="#"
                    ><span>See how Fylo works</span>
                    <img class="icon" src={arrow.src} alt="icon" /></a
                >
            </div>
        </div>
        <Testimonial />
    </div>
</section>

<style>
    section {
        background-color: var(--dark-blue-main);
        color: white;
        text-align: center;
        padding-bottom: 7rem;
    }

    .main-content-img {
        height: 100%;
        padding-inline: 1rem;
        margin-bottom: 3rem;
    }

    .main-content-text {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        align-items: start;
        text-align: start;
    }

    h2 {
        font-size: 1.1rem;
    }

    .icon {
        height: 100%;
        transition: transform 200ms ease-in-out;
    }

    a {
        display: flex;
        align-items: center;
        gap: 0.3rem;
        color: var(--cyan);
        border-bottom: 1px solid var(--cyan);
    }

    a:hover .icon {
        transform: translateX(5px);
    }
</style>
